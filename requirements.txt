# Direct dependencies only — transitive deps are resolved by pip.
# Authoritative list lives in pyproject.toml; keep both in sync.
ansible~=9.1
ansible-runner~=2.3
cryptography>=44.0
flask~=3.0
flask-cors~=4.0
gevent>=24.11
gunicorn~=23.0
Jinja2~=3.1
jmespath
kubernetes
netaddr
PyYAML~=6.0
